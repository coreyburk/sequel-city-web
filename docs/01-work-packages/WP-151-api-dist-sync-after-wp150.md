# WP-151: API Dist Sync After WP-150

**Status:** Accepted
**Owner:** Codex
**Created:** 2026-06-30

## Objective

Synchronize tracked API `dist` output that was legitimately regenerated by WP-150 build stabilization.

## Scope

### In Scope

- review tracked `apps/api/dist/**` diffs after WP-150
- commit substantive generated JavaScript output that mirrors accepted WP-150 source/config behavior
- document line-ending-only tracked `dist` noise separately from substantive generated changes

### Out of Scope

- changing API source files
- changing TypeScript build configuration
- changing frontend code
- changing database files
- changing `.gitignore`, `.gitattributes`, or line-ending policy
- untracking `apps/api/dist/**`

## Impact Analysis

### Understand Status

- Graph available: Yes
- Baseline commit: `834216bd32ffb567db572e725908d2e54c795e9d`
- Freshness assessment: Structurally stale for recent API generated output, but unnecessary for this generated-output sync.
- Analysis performed: Optional-tier generated-output review. `git diff --stat -- apps/api/dist` and `git diff --numstat -- apps/api/dist` identified eight substantive generated files. Remaining tracked `dist` status entries showed no textual diff and are treated as line-ending noise.

### Affected Architecture

- Layers: generated API runtime output
- Primary files/components:
  - `apps/api/dist/app.js`
  - `apps/api/dist/db/sqlServerPool.js`
  - `apps/api/dist/routes/healthRoutes.js`
  - `apps/api/dist/routes/queryHistoryRoutes.js`
  - `apps/api/dist/routes/schemaRoutes.js`
  - `apps/api/dist/services/databaseMetadataService.js`
  - `apps/api/dist/services/queryExecutionService.js`
  - `apps/api/dist/services/schemaService.js`
- Upstream consumers:
  - `npm run start --workspace apps/api`
  - root `npm run start`
- Downstream dependencies:
  - generated CommonJS API runtime output only

### Regression Surface

- Related tests:
  - already verified in WP-150 with `npm run test --workspace apps/api`
  - already verified in WP-150 with `npm run build --workspace apps/api`
  - already verified in WP-150 with root `npm run build`
- User workflows:
  - running compiled API output through `node dist/server.js`
- Security/data boundaries:
  - no source, SQL safety, restricted-table, answer-key, database, or frontend behavior changes

### Graph Update Decision

- Regeneration required: No
- Rationale: Generated output mirrors accepted source/build behavior and does not change source architecture.

## Files Allowed to Change

Allowed:

- `apps/api/dist/app.js`
- `apps/api/dist/db/sqlServerPool.js`
- `apps/api/dist/routes/healthRoutes.js`
- `apps/api/dist/routes/queryHistoryRoutes.js`
- `apps/api/dist/routes/schemaRoutes.js`
- `apps/api/dist/services/databaseMetadataService.js`
- `apps/api/dist/services/queryExecutionService.js`
- `apps/api/dist/services/schemaService.js`
- `docs/01-work-packages/WP-151-api-dist-sync-after-wp150.md`

Do Not Modify:

- `apps/api/src/**`
- `apps/web/**`
- `database/**`
- `docs/00-ssot/**`
- `.gitignore`
- `.gitattributes`
- other `apps/api/dist/**` files that only show line-ending noise

## Constraints

- Commit only substantive generated output that has visible textual diffs.
- Do not commit line-ending-only `dist` noise.
- Do not change source, frontend, database, or repository ignore policy.
- Preserve WP-150 accepted source behavior.

## Required Behavior

- Tracked generated API output matches the WP-150 build output for files with substantive diffs.
- Line-ending-only `dist` files remain out of the commit.
- The repository history records generated-output sync separately from source/build stabilization.

## Acceptance Criteria

- [x] Substantive generated `apps/api/dist` diffs are identified.
- [x] Line-ending-only `apps/api/dist` noise is excluded.
- [x] No API source files are modified in this WP.
- [x] No frontend, database, SSOT, ignore-policy, or line-ending-policy files are modified.
- [x] WP-151 documents the generated-output sync decision.

## Code Prompt

Synchronize the tracked API `dist` output after WP-150.

1. Review `git diff --stat -- apps/api/dist` and `git diff --numstat -- apps/api/dist`.
2. Stage only substantive generated output files with textual diffs.
3. Do not stage line-ending-only `dist` files.
4. Do not modify source, frontend, database, or ignore policy.
5. Commit the generated-output sync separately from WP-150 source work.

## Audit Prompt

Audit WP-151 for generated-output scope compliance.

Verify:

1. Only allowed substantive `apps/api/dist` files and this WP document are committed.
2. Line-ending-only `dist` files are not committed.
3. No source, frontend, database, SSOT, ignore-policy, or line-ending-policy files changed.
4. The committed generated output corresponds to WP-150 build output.

Output:

- Verdict: PASS or FAIL
- Scope violations
- Generated-output drift risks

## Code Results

Implemented.

- Identified eight substantive generated output files under `apps/api/dist/**`.
- Kept line-ending-only tracked `dist` status entries out of scope.
- Prepared a separate generated-output sync record so WP-150 source work remains distinct from emitted runtime output.

## Audit Results

Self-audit completed.

- Verdict: PASS
- Scope compliance: PASS. Intended commit scope is limited to eight generated API output files and this WP document.
- Runtime-boundary preservation: PASS. No source, frontend, database, SSOT, ignore-policy, or line-ending-policy files are part of this WP.
- Drift risk: Low. The generated output reflects the accepted WP-150 TypeScript build behavior, primarily rewritten relative import extensions.

## Final Decision

Accepted.

- Commit the substantive WP-150 API `dist` generated-output sync separately.
- Leave remaining line-ending-only tracked `dist` status entries uncommitted.
